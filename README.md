# Manu — Senior Salesforce Developer (Portfolio)

Open `index.html` in a browser to view the portfolio. Files added:

- `index.html` — main site
- `styles.css` — styles and responsive layout
- `script.js` — minimal JS for mobile nav and active link

No build step required — static site. To preview quickly, open the file in your browser or use a local static server like `http-server` or `python -m http.server`.
